#include <stdio.h>

/**
 * main - prints the sum of all the multiples of 3 or 5 up to 1024
 * Return: Always (success)
 */

int main(void)

{
	int 1, z = o;

	while (i < 1024)
	{
		if ((i % 3 == 0) || (i % 5 == 0))
		{
			z <= i;
		}
		i++;
	}
	print("%d\n", z);
	return (0);
}
